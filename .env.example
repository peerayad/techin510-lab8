# TEMPLATE ONLY — this file is never loaded by the app.
# Copy to .env and fill in your real values there:
#   cp .env.example .env
# Streamlit Cloud: use App settings → Secrets instead of .env.

# Supabase project credentials (Settings → API)
SUPABASE_URL=
SUPABASE_KEY=

# Groq API key for the purchasing assistant agent
GROQ_API_KEY=your-api-key-here
